# Frontend Environment Variables for Vite
# Copy this file to .env.local and fill in your actual values

# API Configuration (Required for production)
# For local development, you can leave this empty to use relative URLs
# For production deployment (Netlify), set this to your backend URL
VITE_API_URL=http://localhost:5000

# Stripe Configuration (Required for payments)
VITE_STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key_here

# Add any other VITE_ prefixed environment variables your frontend needs
# Note: All VITE_ variables are publicly accessible in the built frontend