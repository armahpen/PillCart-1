# Backend Environment Variables for Render
# Copy these to your Render dashboard under Environment Variables

# Database Configuration (Required)
# This will be automatically set by Render when you create a PostgreSQL database
DATABASE_URL=postgresql://username:password@host:port/database

# Node Environment (Required)
NODE_ENV=production

# Server Configuration
PORT=5000

# Frontend URL (Required for CORS)
# Replace with your actual Netlify frontend URL after deployment
FRONTEND_URL=https://your-frontend-name.netlify.app

# Session Secret (Required for authentication)
# Render can auto-generate this, or set your own secure random string
SESSION_SECRET=your_super_secure_session_secret_here

# Stripe Configuration (Required for payments)
# Get these from your Stripe dashboard
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Optional: Google Cloud Storage (for file uploads)
GOOGLE_CLOUD_PROJECT_ID=your_project_id
GOOGLE_CLOUD_STORAGE_BUCKET=your_bucket_name
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# Optional: Email service (if you add email functionality)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password